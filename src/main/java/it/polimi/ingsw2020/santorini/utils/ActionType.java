package it.polimi.ingsw2020.santorini.utils;

public enum ActionType {
    SELECT_BUILDER,
    SELECT_CELL_MOVE,
    MOVE,

    SELECT_CELL_BUILD,
    BUILD,

    ACTIVATE_GOD,
    SELECT_PARAMETERS,
    APOLLO_PARAMETERS,
    USE_POWER;
}
